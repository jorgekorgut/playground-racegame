#pragma once
#include <scene/GameObject.h>
#include <Engine.h>

class Terrain {
public:
	Terrain(GameObject & gameObject)
	{
		/*
		std::vector<glm::vec3> vertices = {
			glm::vec3(0.5f, 0.0f, -0.5f),
			glm::vec3(0.5f, 0, 0.5f),
			glm::vec3(-0.5f, 0, 0.5f),
			glm::vec3(-0.5f, 0, -0.5f)
		};

		gameObject.model->meshes[0].UpdateVertexPosition(vertices);
		*/
	}
};